# RagGit
RagGit is a Retrieval-Augmented Generation (RAG) system that enables users to query GitHub repositories using natural language. It combines semantic search (FAISS + embeddings) with LLMs (Groq LLaMA) to deliver context-aware, accurate answers through an interactive Streamlit interface.
